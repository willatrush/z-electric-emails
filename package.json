{
  "name": "z-electric-emails",
  "version": "1.0.0",
  "description": "This README would normally document whatever steps are necessary to get your application up and running.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "dev": "yarn clear && yarn build && open ./liquid/sign_up_confirmation.html && yarn watch",
    "build": "yarn clear && npx mjml ./mjml/*.mjml -o ./liquid/",
    "watch": "yarn clear && npx mjml -w ./mjml/*.mjml -o ./liquid/",
    "clear": "rm -rf liquid && mkdir liquid"
  },
  "repository": {
    "type": "git",
    "url": "git+https://willrush@bitbucket.org/rushdigital/z-electric-emails.git"
  },
  "author": "",
  "license": "ISC",
  "homepage": "https://bitbucket.org/rushdigital/z-electric-emails#readme",
  "dependencies": {
    "fs-extra": "^9.0.0",
    "jest": "^25.2.7",
    "jest-image-snapshot": "^3.0.1",
    "liquid": "^4.0.1",
    "mjml": "^4.6.2",
    "puppeteer": "^2.1.1"
  }
}
